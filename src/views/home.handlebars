<h1>Lista de Productos</h1>
{{#each products}}
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <p>{{this.price}}</p>
    <p>{{this.category}}</p>
    <p>{{this.code}}</p>
    <p>{{this.stock}}</p>
{{/each}}
